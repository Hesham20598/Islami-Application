<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IslamiApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/gold_color</item>
    </style>

    <style name="Theme.IslamiApplication" parent="Base.Theme.IslamiApplication" />

    <style name="bottomNavigationStyle">
        <item name="itemActiveIndicatorStyle">@null</item>
        <item name="itemBackground">@null</item>
        <item name="android:background">@color/gold_color</item>
        <item name="itemIconSize">40dp</item>
        <item name="itemIconTint">@color/item_bottom_navigation_view_selector_color</item>
        <item name="itemTextColor">@color/item_bottom_navigation_view_selector_color</item>
    </style>
</resources>